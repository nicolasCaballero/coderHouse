<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
</head>

<body class="p-3 mb-2 bg-secondary text-white">
    <div class="container">
        <h1 class="p-3 mb-2 bg-dark text-white text-center border border-white shadow p-3 mb-5 rounded"> VISTA PRODUCTOS </h1>
        <% if (products.length > 0) { %>
        <table class="table table-dark">
            <thead>
                <tr>
                    <th scope="col">NOMBRE</th>
                    <th scope="col">PRECIO</th>
                    <th scope="col">IMAGEN</th>
                </tr>
            </thead>
            <tbody>
                <% for( let i = 0; i < products.length; i++ ) { %>
                <tr>
                    <td><%= products[i].title %></td>
                    <td>$<%= products[i].price %></td>
                    <td><img height="100" width="100" src="<%= products[i].thumbnail %>" class="img-thumbnail"></td>
                </tr>
                <% } %>
            </tbody>
        </table>
        <div class="container">
            <a href="http://localhost:3434/"> <button class="btn btn-dark border border-white shadow rounded">VOLVER</button></a>
        </div>
        <% } else { %>
        <p class="alert alert-danger">No se encontraron datos</p>
    </div>
    <div class="container">
        <a href="http://localhost:3434/"><button class="btn btn-dark border border-white shadow rounded">VOLVER</button></a>
    </div>
    <% } %>
</body>
</html>